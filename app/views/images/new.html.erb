<h1>New Image</h1>

<%= error_messages_for :image %>

<% form_for(:image, :url => collection_url, :html => { :multipart => true}) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
<br/>
<%= link_to 'Back', collection_url %>